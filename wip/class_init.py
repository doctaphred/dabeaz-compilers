class Ayy:
    class __init__(stuff):
        def x(obj):
            assert isinstance(obj, int)
            return obj

        def y(obj):
            assert isinstance(obj, str)
            assert len(obj) == 1
            return ord(obj)

    class x:
        def __init__(self, value):
            pass

        def type(self):
            pass
